import React from 'react';
import styled from 'styled-components';

const StyledTitle =  styled.h2`

`;
const Title = props =>  <StyledTitle className='title-calculator cool-link'>{props.title}</h2> 

export default Title;